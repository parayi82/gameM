{
  "name": "la-cabana",
  "version": "0.1.0",
  "private": true,
  "description": "La Cabaña — novela interactiva room-escape, motor genérico + capítulos como datos JSON",
  "scripts": {
    "dev": "npx --yes serve . -l 8080",
    "generate-art": "node scripts/generate-art.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^16.12.0"
  }
}
