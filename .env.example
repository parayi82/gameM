# --- Frontend (public, safe to expose) ---
# Copiar a config.local.js si se quiere sobreescribir config.js en el navegador
SUPABASE_URL=
SUPABASE_ANON_KEY=
STRIPE_PUBLISHABLE_KEY=

# --- Netlify Functions (privado, NUNCA exponer al cliente) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
SUPABASE_SERVICE_ROLE_KEY=
SITE_URL=http://localhost:8080

# --- Pipeline de arte (offline, no se usa en runtime) ---
GEMINI_API_KEY=
